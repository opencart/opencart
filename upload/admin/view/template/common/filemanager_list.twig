<div class="row mb-3">
  <div class="col-sm-5">
    <a href="{{ parent }}" data-bs-toggle="tooltip" title="{{ button_parent }}" class="btn btn-light" id="button-parent"><i class="fa-solid fa-level-up-alt"></i></a>
    <a href="{{ refresh }}" data-bs-toggle="tooltip" title="{{ button_refresh }}" class="btn btn-light" id="button-refresh"><i class="fa-solid fa-rotate"></i></a>
    <button type="button" data-bs-toggle="tooltip" title="{{ button_upload }}" class="btn btn-primary" id="button-upload"><i class="fa-solid fa-upload"></i></button>
    <button type="button" data-bs-toggle="tooltip" title="{{ button_folder }}" class="btn btn-light" id="button-folder"><i class="fa-solid fa-folder"></i></button>
    <button type="button" data-bs-toggle="tooltip" title="{{ button_delete }}" class="btn btn-danger" id="button-delete"><i class="fa-regular fa-trash-can"></i></button>
    <input type="hidden" name="directory" value="{{ directory }}" id="input-directory"/>
  </div>
  <div class="col-sm-7">
    <div class="input-group">
      <input type="text" name="search" value="{{ filter_name }}" placeholder="{{ entry_search }}" class="form-control" id="input-search">
      <button type="button" data-bs-toggle="tooltip" title="{{ button_search }}" class="btn btn-primary" id="button-search"><i class="fa-solid fa-search"></i></button>
    </div>
  </div>
</div>
<div class="row mb-3" style="display: none;" id="modal-folder">
  <div class="col-sm-12">
    <div class="input-group">
      <div class="input-group">
        <input type="text" name="folder" value="" placeholder="{{ entry_folder }}" class="form-control" id="input-folder">
        <button type="button" title="{{ button_folder }}" class="btn btn-primary" id="button-create"><i class="fa-solid fa-plus-circle"></i></button>
      </div>
    </div>
  </div>
</div>
<hr/>
<div class="row row-cols-sm-3 row-cols-lg-4">
  {% set path_row = 0 %}
  {% for directory in directories %}
    <div class="mb-3">
      <div class="mb-1" style="min-height: 140px;">
        <a href="{{ directory.href }}" class="directory mb-1"><i class="fa-solid fa-folder fa-5x"></i></a>
      </div>
      <div class="form-check">
        <label for="input-path-{{ path_row }}" class="form-check-label">{{ directory.name }}</label>
        <input type="checkbox" name="path[]" value="{{ directory.path }}" class="form-check-input" id="input-path-{{ path_row }}"/>
      </div>
    </div>
    {% set path_row = path_row + 1 %}
  {% endfor %}
  {% for image in images %}
    <div class="mb-3">
      <div class="mb-1" style="min-height: 140px;">
        <a href="{{ image.href }}" class="thumbnail mb-1"><img src="{{ image.thumb }}" alt="{{ image.name }}" title="{{ image.name }}" class="img-fluid"/></a>
      </div>
      <div class="form-check">
        <label for="input-path-{{ path_row }}" class="form-check-label">{{ image.name }}</label>
        <input type="checkbox" name="path[]" value="{{ image.path }}" class="form-check-input" id="input-path-{{ path_row }}"/>
      </div>
    </div>
    {% set path_row = path_row + 1 %}
  {% endfor %}
</div>
{% if pagination %}
  <div class="modal-footer">{{ pagination }}</div>
{% endif %}