<h1>{{ text_address_book }}</h1>
<div id="address">{{ list }}</div>
<div class="row">
  <div class="col">
    <a href="{{ back }}" class="btn btn-light">{{ button_back }}</a>
  </div>
  <div class="col text-end">
    <a href="{{ add }}" class="btn btn-primary">{{ button_address_add }}</a>
  </div>
</div>