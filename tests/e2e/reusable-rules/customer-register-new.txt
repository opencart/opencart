// OpenCart logs the customer in automatically on success, so no follow-up login is needed.
click "My Account"
click "Register"
check that page contains "Register Account"
enter "Test" into "First Name"
generate unique name, then enter into "Last Name" and save as "customerLastName"
generate unique email, then enter into "E-Mail" and save as "customerEmail"
// Generated per run and saved as "currentPassword", which tracks whatever password this
// account currently has. Upper, lower, digit and symbol, so it satisfies any combination
// of OpenCart's configurable password rules.
generate by regex "[A-Z][a-z]{6}[0-9]{2}[!@#]", then enter into "Password" and save as "currentPassword"
// The agree toggle sits to the RIGHT of its label here, but to the LEFT on the checkout
// register form - do not reuse this rule there.
click on the checkbox to the right of "I have read and agree to the"
click "Continue"
check that page contains "Your Account Has Been Created!"
