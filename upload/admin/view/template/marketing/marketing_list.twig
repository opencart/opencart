<form id="marketing-list" data-load="{{ action }}" data-target="#marketing">
	<div class="table-responsive">
		<table class="table table-bordered table-hover">
			<thead>
				<tr>
					<td style="width: 1px;" class="text-center"><input type="checkbox" onclick="$('input[name*=\'selected\']').prop('checked', $(this).prop('checked'));"/></td>
					<td class="text-left">{% if sort == 'm.name' %} <a href="{{ sort_name }}" class="{{ order|lower }}">{{ column_name }}</a> {% else %} <a href="{{ sort_name }}">{{ column_name }}</a> {% endif %}</td>
					<td class="text-left">{% if sort == 'm.code' %} <a href="{{ sort_code }}" class="{{ order|lower }}">{{ column_code }}</a> {% else %} <a href="{{ sort_code }}">{{ column_code }}</a> {% endif %}</td>
					<td class="text-right">{{ column_clicks }}</td>
					<td class="text-right">{{ column_orders }}</td>
					<td class="text-left d-none d-lg-table-cell">{% if sort == 'm.date_added' %} <a href="{{ sort_date_added }}" class="{{ order|lower }}">{{ column_date_added }}</a> {% else %} <a href="{{ sort_date_added }}">{{ column_date_added }}</a> {% endif %}</td>
					<td class="text-right">{{ column_action }}</td>
				</tr>
			</thead>
			<tbody>
				{% if marketings %}
					{% for marketing in marketings %}
						<tr>
							<td class="text-center"><input type="checkbox" name="selected[]" value="{{ marketing.marketing_id }}"/></td>
							<td class="text-left">{{ marketing.name }}</td>
							<td class="text-left">{{ marketing.code }}</td>
							<td class="text-right">{{ marketing.clicks }}</td>
							<td class="text-right">{{ marketing.orders }}</td>
							<td class="text-left d-none d-lg-table-cell">{{ marketing.date_added }}</td>
							<td class="text-right"><a href="{{ marketing.edit }}" data-bs-toggle="tooltip" title="{{ button_edit }}" class="btn btn-primary"><i class="fas fa-pencil-alt"></i></a></td>
						</tr>
					{% endfor %}
				{% else %}
					<tr>
						<td class="text-center" colspan="8">{{ text_no_results }}</td>
					</tr>
				{% endif %}
			</tbody>
		</table>
	</div>
	<div class="row">
		<div class="col-sm-6 text-left">{{ pagination }}</div>
		<div class="col-sm-6 text-right">{{ results }}</div>
	</div>
</form>